jest install

jest run:
  jest
  npm run jest:watch

jest API:
  describe(name, fn)
  it(name, fn)
  expect(value).toEqual(value)
  expect(value).toBe(value)

function test:

component test:

enzyme testing utility for react
enzyme install
